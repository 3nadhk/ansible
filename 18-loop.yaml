- name: install packages
  hosts: frontend
  become: yes
  tasks:
  - name: install packages
    ansible.builtin.package:
      name: "{{ item }}"
      stage: installed
    loop:
    - mysql
    - nginx
    - zip

  